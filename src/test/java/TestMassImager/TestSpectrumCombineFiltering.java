package TestMassImager;


public class TestSpectrumCombineFiltering {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		/*
		Window window = new Window();
		window.getFrame().setVisible(true);
		// load data
		// /data/triggerimage/data01.mzXML  to 45.mzXML
		
		
		for(int i=1; i<2; i++) {
			String s = window.getPathOfJar().getPath()+"/data/triggerimage/data";
			if(i<10) s += "0";
			s+= i+".mzXML";
			window.getLogicRunner().loadMzXMLFile(new File(s));
		}
		
		Scan spec = window.getLogicRunner().getListSpecFiles().firstElement().getNearestSpectrumAtRT(1);
		Scan spec2 = window.getLogicRunner().getListSpecFiles().firstElement().getNearestSpectrumAtRT(0.5);
		
		Vector<MsnSpectrum> specList = new Vector<MsnSpectrum>();
		specList.add(spec);
		specList.add(spec2);
		
		MZSpectrumCombineFilter filter = new MZSpectrumCombineFilter(specList);
		filter.doFiltering();
		*/
	}

}
